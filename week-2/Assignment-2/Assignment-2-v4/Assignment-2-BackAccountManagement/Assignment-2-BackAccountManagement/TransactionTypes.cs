namespace Assignment_2_BackAccountManagement
{
    // enum is a special class that represents constants
    // creating enum class TransactionType which contains all transaction types this bank offers
    public enum TransactionType
    {
        Deposit,
        Withdraw,
        BankCharges,
        InterestAccrue
    }
}
